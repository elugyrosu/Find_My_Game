workspace 'P12Workspace.xcworkspace'
platform :ios, '8.0'

target 'P12' do
  pod 'SDWebImage', '~> 5.0', :modular_headers => true

end

target 'P12Tests' do
  pod 'SDWebImage', '~> 5.0', :modular_headers => true

end
